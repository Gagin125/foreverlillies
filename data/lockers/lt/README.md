# Lithuania parcel lockers (sample data)

These JSON files contain a **small sample** of Omniva + DPD parcel locker locations for development.

TODO: Replace `omniva.json` and `dpd.json` with the full official datasets before production.
Keep the JSON structure identical to `ParcelLocker` (see `data/lockers/lt/index.ts`).
